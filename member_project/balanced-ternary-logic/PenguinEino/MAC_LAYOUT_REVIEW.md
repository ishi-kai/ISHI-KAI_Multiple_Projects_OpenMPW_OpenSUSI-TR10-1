# MAC配置整理と独立レビュー（2026-09-25）

対象：`mac.gds` SHA-256 `13a8c4415aac8f88be5a9fc05792727665c7352ef444de89f1c39f2e7bcbafd6`。
変更前：Git `2919b60`。独立レビュアー：サブエージェント `/root/alignment_review`（読み取りのみ）。

## 変更

- 上段のMUL 4ゲートと追加INV 3個を原点y=600 µmの一列に揃えた。セルの高さ自体は異なるため、上端の素子位置まで同じにはしていない。
- 上段・中段のVDD/VSS計4本をM1 44 µmの直線レールに統一。上段端部はx=1480 µmで止め、AND/OR出力も短い引き出しを維持。
- M2幹線14 µm・4接続点各24カットを維持。FA側は32→44 µmに拡幅し、干渉するC1配線を移動。
- 旧40 µmのMUL＋FA共有VSS区間を廃止。上段セル群とFAはそれぞれの水平レールから幹線に接続。
- 外形1774.7×779.35 µm、座標(6.3,12.35)〜(1781,791.7) µm。1800×1000 µm内。DBU=0.001 µm、配置格子0.05 µm、倍率1。
- 118 MOS＋46 RR、素子寸法、HA内部形状、回路機能は維持。

## 検証

| 項目 | 結果 |
|---|---|
| MUL / FA / MAC Drawing DRC・strict LVS | 0件・一致 |
| MAC通常GUIのDRC/LVS・PCell再生成照合 | 合格・図形XOR 0 |
| 実GDSの4レール・2幹線・96ビア・7セル整列 | 合格 |
| 11端子の金属・TXMラベル・座標 | 一致 |
| 抽出81入力＋復帰、新規実行 | 合格、最大4出力誤差61.286 mV、整定414.265 ns |
| 抽出全6,480遷移・単一入力648遷移 | 下記同等性に基づき変更前の合格結果を継承 |
| 提出用GDSの独立Drawing DRC・strict LVS | 0件・一致、編集用GDSと全図形/ラベル一致 |

抽出の全9階層で、formal port名、素子端子の役割（RRのPLUS/MINUSも含む）、モデル、全素子パラメータを保持したグラフ同型性を確認した。PDKモデル・参照回路・ルールも一致。サブエージェントが同型性を独立に再実行して確認した。
全6,480/648遷移は今回再実行したものではない。新規81入力試験は10 pF || 1 MΩ、1 µs保持、1 ns入力エッジ。

単体マスクDRCの外部入力由来Floating SG 14件は残る。入力接続診断fixtureではDrawing/LVS/マスクDRCすべて0件。実フレーム接続後の確認は別途必要。
抽出は配線寄生R/Cを含まない。電源配線の確認は公称±5 Vの既存DC電流集計との照合で、正式なAC/EM/PEX判定や実遅延改善の主張ではない。

## レビュー結果

**整理後の配置・接続・電源経路・検証方法に修正必須の問題なし。最終的な未解決指摘は0件。**

1. 旧40 µm共有VSS区間の説明が現行として残っているとの軽微指摘：現構造へ文書更新し、旧`power_join.json`を履歴と明示して解消。
2. 更新文書が参照する`power_geometry.json`が未生成との追加指摘：実GDS検査スクリプトで生成し、レビュアーが現GDSハッシュとの一致と検査内容を確認して解消。

証跡：

- [電気的同等性と新規81入力](reports/mac_alignment/electrical_equivalence.json)
- [主電源と配置の実形状検査](reports/mac_alignment/power_geometry.json)
- [物理検証](reports/mac_layout.json) / [入力接続fixture](reports/mac_driver.json)
- [提出GDS検証](reports/submission_layout.json)
- [更新レイアウト画像](submission/mac_layout.png) / [端子図](submission/mac_pins.png)

既存の電源定格・フレーム統合・製造条件については[MAC_IMPROVEMENTS.md](MAC_IMPROVEMENTS.md)を参照。

## レビュー後の追加変更：下段電源レールの橋渡し

ユーザー追加指示により、左右HA間のVSS/VDD/VSSと、右HAからcarry合成部への最下段VSSを親FAで接続した。上記独立レビューは橋渡し前のハッシュを対象とする。追加4矩形以外の図形不変、Drawing DRC/LVS、全9階層の抽出端子・全パラメータ同等性を主エージェントが確認。過渡結果は同等性に基づく継承で、追加の独立レビューや過渡再実行は行っていない。
証跡：[橋渡し検証](reports/mac_alignment/rail_stitch.json)。更新画像・提出用GDSも橋渡し後の版に更新。

## 追加変更：右端のVDDも接続

さらに右HAのVDDからcarry合成NANYへ12 µm幅で橋渡しし、NANYから右端INVへ3.4 µm幅の段差配線で接続した。
C1はこの交差点でM2を通るため、不要になった旧ビア（FA座標1308.45,22.35 µm）を除去した。既存の個別給電は維持。
追加接続後もDrawing DRC/LVSと全9階層の抽出同等性に合格。過渡結果は継承し、配線RCによる改善量は未評価。
証跡は `reports/mac_alignment/rightmost_vdd.json`。
この追加変更の確認は主エージェントによるもので、上記の独立レビュー対象ハッシュとは別版。
